extends layout

block content
    h1= title
    p Welcome to #{title}

block user
    if user
        li
            a(href='/edit-profile') Profile
        li
            a(href='/logout') Logout
    else
        li
            a(href='/login') Login